<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="order_lunch.list">
      <field name="name">order_lunch list</field>
      <field name="model">order_lunch.order_lunch</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="order_lunch.action_window">
      <field name="name">order_lunch window</field>
      <field name="res_model">order_lunch.order_lunch</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="order_lunch.action_server">
      <field name="name">order_lunch server</field>
      <field name="model_id" ref="model_order_lunch_order_lunch"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="order_lunch" id="order_lunch.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="order_lunch.menu_1" parent="order_lunch.menu_root"/>
    <menuitem name="Menu 2" id="order_lunch.menu_2" parent="order_lunch.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="order_lunch.menu_1_list" parent="order_lunch.menu_1"
              action="order_lunch.action_window"/>
    <menuitem name="Server to list" id="order_lunch" parent="order_lunch.menu_2"
              action="order_lunch.action_server"/>
    -->
  </data>
</odoo>