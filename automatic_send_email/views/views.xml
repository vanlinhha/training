<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="automatic_send_email.list">
      <field name="name">automatic_send_email list</field>
      <field name="model">automatic_send_email.automatic_send_email</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="automatic_send_email.action_window">
      <field name="name">automatic_send_email window</field>
      <field name="res_model">automatic_send_email.automatic_send_email</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="automatic_send_email.action_server">
      <field name="name">automatic_send_email server</field>
      <field name="model_id" ref="model_automatic_send_email_automatic_send_email"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="automatic_send_email" id="automatic_send_email.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="automatic_send_email.menu_1" parent="automatic_send_email.menu_root"/>
    <menuitem name="Menu 2" id="automatic_send_email.menu_2" parent="automatic_send_email.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="automatic_send_email.menu_1_list" parent="automatic_send_email.menu_1"
              action="automatic_send_email.action_window"/>
    <menuitem name="Server to list" id="automatic_send_email" parent="automatic_send_email.menu_2"
              action="automatic_send_email.action_server"/>
    -->
  </data>
</odoo>